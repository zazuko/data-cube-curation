import { Request, Response, NextFunction } from 'express'

export function createFactTable (req: Request, res: Response, next: NextFunction) {
  next()
}

export function get (req: Request, res: Response, next: NextFunction) {
  next()
}
